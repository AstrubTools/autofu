function inject (bot) {
// Ranking from highest level to lowest level
  bot.xpJob = async (job) => {
  }

  bot.gatherExpensive = async (jobs) => {
  }

  bot.getJob = async (job) => {
  }

  bot.gatherResource = async (resource) => {
  }
}

module.exports = inject
