// Ranking from highest level to lowest level
function inject (bot) {
// E.g Bonta, Brakmar, dungeon black rat, Otomaï ...
  bot.moveToLocation = async (location) => {
  }

  // E.g -25; 21
  bot.moveToCoordinates = async (coordinates) => {
  }

  // E.g cell 213
  bot.moveToCell = async (cell) => {
  }
}

module.exports = inject
